function computeAverageLengthOfWords(word1, word2) {
  var lengtha = word1.length;
  var lengthb = word2.length;
  var added = lengtha + lengthb;
  var final = added / 2;
  return(final)
  // your code here
}

computeAverageLengthOfWords('code', 'programs');
